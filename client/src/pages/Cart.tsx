import { Button } from "@/components/ui/button";
import { useCart } from "@/contexts/CartContext";
import { ArrowRight, Minus, Plus, Trash2 } from "lucide-react";
import { Link } from "wouter";

export default function Cart() {
  const { items, total, setQuantity, remove } = useCart();
  return <div className="container py-14 sm:py-20"><div className="flex items-end justify-between border-b border-[#20241f]/15 pb-8"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-[#e35f36]">Your edit</p><h1 className="mt-3 font-serif text-6xl tracking-[-0.05em]">Your bag.</h1></div><span className="text-sm text-[#77776f]">{items.reduce((sum, item) => sum + item.quantity, 0)} items</span></div>{items.length === 0 ? <div className="py-24 text-center"><div className="mx-auto h-16 w-16 rounded-full bg-[#d9e7d2]" /><h2 className="mt-7 font-serif text-4xl">Nothing here yet.</h2><p className="mt-3 text-[#77776f]">The good stuff is waiting to be discovered.</p><Button asChild className="mt-7 rounded-full bg-[#20241f] px-7 hover:bg-[#e35f36]"><Link href="/catalog">Start shopping <ArrowRight size={16} /></Link></Button></div> : <div className="grid gap-12 py-10 lg:grid-cols-[1fr_340px] lg:items-start"><div className="space-y-5">{items.map(item => <div key={item.id} className="flex gap-4 rounded-[1.5rem] bg-[#e8e4db] p-4 sm:gap-6 sm:p-5"><img src={item.imageUrl || "https://images.unsplash.com/photo-1494438639946-1ebd1d20bf85?auto=format&fit=crop&w=300&q=80"} alt={item.name} className="h-28 w-24 rounded-2xl object-cover sm:h-36 sm:w-32" /><div className="flex min-w-0 flex-1 flex-col justify-between py-1"><div className="flex justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-[#77776f]">{item.category || "Selected"}</p><h2 className="mt-1 font-serif text-2xl">{item.name}</h2></div><p className="font-bold">${(Number(item.price) * item.quantity).toFixed(2)}</p></div><div className="flex items-center justify-between"><div className="flex items-center gap-1 rounded-full border border-[#20241f]/15 p-1"><button aria-label="Decrease quantity" onClick={() => item.quantity > 1 ? setQuantity(item.id, item.quantity - 1) : remove(item.id)} className="rounded-full p-1.5 hover:bg-[#f6f3ee]"><Minus size={13} /></button><span className="w-6 text-center text-sm">{item.quantity}</span><button aria-label="Increase quantity" onClick={() => setQuantity(item.id, item.quantity + 1)} className="rounded-full p-1.5 hover:bg-[#f6f3ee]"><Plus size={13} /></button></div><button onClick={() => remove(item.id)} className="flex items-center gap-1 text-xs font-semibold text-[#77776f] hover:text-[#e35f36]"><Trash2 size={14} /> Remove</button></div></div></div>)}</div><aside className="rounded-[1.75rem] bg-[#20241f] p-7 text-[#f6f3ee] lg:sticky lg:top-28"><p className="text-xs font-bold uppercase tracking-[0.2em] text-[#d9e7d2]">Summary</p><div className="mt-7 space-y-4 text-sm"><div className="flex justify-between text-white/65"><span>Subtotal</span><span>${total.toFixed(2)}</span></div><div className="flex justify-between text-white/65"><span>Shipping</span><span>{total >= 75 ? "Free" : "$6.00"}</span></div><div className="border-t border-white/15 pt-4"><div className="flex justify-between text-base font-bold"><span>Total</span><span>${(total >= 75 ? total : total + 6).toFixed(2)}</span></div></div></div><Button asChild className="mt-8 h-12 w-full rounded-full bg-[#f2a88f] font-bold text-[#20241f] hover:bg-white"><Link href="/checkout">Continue to checkout <ArrowRight size={16} /></Link></Button><p className="mt-4 text-center text-xs text-white/40">Secure checkout · Demo payment ready</p></aside></div>}</div>;
}
